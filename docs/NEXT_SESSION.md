# Next Session — Pending Items

Items to action at the start of the next session. Clear this file once actioned.

## Pending investigations

| # | Item |
|---|---|
| 1 | **Template repo not fetchable by Clead** — `sugose/ai-project-template` returns 404 when Clead tries to `web_fetch` a PR URL directly. Likely a GitHub indexing delay on newly-public template repos. Retest in a future session by dropping a PR URL from that repo into Clead's chat. |
